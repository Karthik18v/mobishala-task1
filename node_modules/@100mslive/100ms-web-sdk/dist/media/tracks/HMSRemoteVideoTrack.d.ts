import HMSVideoTrack from './HMSVideoTrack';
import { HMSSimulcastLayer } from '../settings';
export default class HMSRemoteVideoTrack extends HMSVideoTrack {
    setEnabled(value: boolean): Promise<void>;
    preferLayer(layer: HMSSimulcastLayer): Promise<void>;
    addSink(videoElement: HTMLVideoElement): Promise<void>;
    removeSink(videoElement: HTMLVideoElement): Promise<void>;
}
