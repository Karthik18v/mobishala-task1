import HMSMessage from '../../interfaces/message';
import { HMSMessageType } from './enums/HMSMessageType';
interface HMSMessageInit {
    sender: string;
    message: string;
    type: string;
    receiver?: string;
    time?: Date | string;
}
export default class Message implements HMSMessage {
    sender: string;
    receiver: string;
    message: string;
    time: Date;
    type: HMSMessageType;
    constructor({ sender, message, type, receiver, time }: HMSMessageInit);
}
export {};
