import HMSSpeaker from '../../interfaces/speaker';
export declare class Speaker implements HMSSpeaker {
    peerId: string;
    trackId: string;
    audioLevel: number;
    constructor(peerId: string, trackId: string, audioLevel: number);
}
export declare class SpeakerList {
    speakers: HMSSpeaker[];
    constructor(speakerList: any);
}
