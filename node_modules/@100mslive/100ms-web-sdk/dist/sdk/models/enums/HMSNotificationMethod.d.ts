export declare enum HMSNotificationMethod {
    PEER_JOIN = 0,
    PEER_LEAVE = 1,
    PEER_LIST = 2,
    ACTIVE_SPEAKERS = 3,
    BROADCAST = 4,
    ROLE_CHANGE = 5,
    TRACK_METADATA_ADD = 6,
    TRACK_UPDATE = 7,
    UNSUPPORTED = 8
}
export declare const getNotificationMethod: (method: string) => HMSNotificationMethod;
